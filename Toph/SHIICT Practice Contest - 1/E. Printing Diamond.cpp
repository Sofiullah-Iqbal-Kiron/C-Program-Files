#include<bits/stdc++.h>
#define START int main(){
#define END system("PAUSE");return 0;}
using namespace std;

START

    int n, r, c, i, j, m;
    cin >> n;
    m=(n+1)/2;
    for(r=1; r<=m; r++)
    {
        if(r%2!=0)
        {
            for(c=m; c>=m-; c++)
            {
                if()
            }
        }
    }

END
