/*
    ->Sets are a type of associative container in which
    elements has to be unique.
    ->Sets are containers that stores unique elements
    following a specific order. So, if we insert an element
    twice, container does not take the value second time.
*/

#include<iostream>
#include<set> //Access set.
using namespace std;

int main()
{
    cout << ~5;
}